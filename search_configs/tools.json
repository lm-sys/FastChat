[
        {
            "type": "function",
            "function": {
                "name": "web_search",
                "description": "Retrieve relevant websites based on provided keywords. This function should be invoked whenever answering the problem requires up-to-date information or specific details that are not available within existing knowledge.",
                "parameters": {
                    "type": "object",
                    "properties": {
                        "key_words": {
                            "type": "string",
                            "description": "The key words to search for."
                        },
                        "topk": {
                            "type": "integer",
                            "description": "The number of search results to return (default value set to 1)."
                        }
                    },
                    "required": ["key_words", "topk"]
                }
            }
        }
]
